const client = {
  addRow: (api, row) => {},
  addRows: (api, rows = []) => {},
  editRow: (api, row) => {},
  editRows: (api, rows = []) => {},
  removeRow: (api, row) => {},
  removeRows: (api, rows) => {}
};
export default { ...client };
